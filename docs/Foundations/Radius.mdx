import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Foundations/Radius" />

# Radius

Radius communicates the shape and interaction model of an element. Use the component token rather than setting a one-off radius. Keep controls and adjacent elements visually related.