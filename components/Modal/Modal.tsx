import type { ReactNode } from 'react';
import { useEffect, useRef } from 'react';
import '../shared/components.css';
export function Modal({ children, onClose, open, title }: { children: ReactNode; onClose: () => void; open: boolean; title: string }) { const closeRef = useRef<HTMLButtonElement>(null); useEffect(() => { if (open) closeRef.current?.focus(); }, [open]); if (!open) return null; return <div aria-label={title} aria-modal="true" className="ds-modal__backdrop" role="dialog" onMouseDown={(event) => { if (event.target === event.currentTarget) onClose(); }}><div className="ds-modal"><header className="ds-modal__header"><h2 className="ds-modal__title">{title}</h2><button aria-label="Close dialog" className="ds-modal__close" onClick={onClose} ref={closeRef} type="button">×</button></header><div className="ds-modal__body">{children}</div><footer className="ds-modal__footer"><button className="ds-button ds-button--outlined ds-button--m" onClick={onClose} type="button">Cancel</button><button className="ds-button ds-button--filled ds-button--primary ds-button--m" onClick={onClose} type="button">Confirm</button></footer></div></div>; }